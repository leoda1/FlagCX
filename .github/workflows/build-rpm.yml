name: Build RPM Packages

on:
  push:
    tags:
      - 'v*'
  pull_request:
    branches: [ main ]
    paths:
      - 'flagcx/**'
      - 'packaging/rpm/**'
      - '.github/workflows/build-rpm.yml'
  workflow_dispatch:

jobs:
  # Build on GitHub-hosted runners. The RPMs are produced entirely inside
  # backend-specific Docker images pulled from public registries (nvcr.io,
  # Docker Hub, repos.metax-tech.com), so the build is host-agnostic and needs
  # neither the internal h20 runner nor a real GPU. This keeps PR checks green
  # without depending on the internal network's flaky public egress.
  #
  # ascend targets aarch64 (the CANN 910 base image is arm64), so it runs on an
  # arm64 runner; the other targets build x86_64 packages.
  build-rpm-packages:
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: nvidia
            backend: nvidia
            distro: rocky8
            base_image: nvcr.io/nvidia/cuda
            base_image_version: 12.4.1-devel-rockylinux8
            runner: ubuntu-latest
          - target: nvidia-openeuler2403
            backend: nvidia
            distro: openeuler2403
            base_image: openeuler/cuda
            base_image_version: 13.0.0-oe2403lts
            runner: ubuntu-latest
          - target: nvidia-fedora43
            backend: nvidia
            distro: fedora43
            base_image: fedora
            base_image_version: '43'
            runner: ubuntu-latest
          - target: metax
            backend: metax
            distro: rocky8
            base_image: rockylinux
            base_image_version: '8'
            runner: ubuntu-latest
          - target: ascend
            backend: ascend
            distro: openeuler2403
            base_image: ascendai/cann
            base_image_version: 8.5.0-910-openeuler24.03-py3.11
            runner: ubuntu-24.04-arm
    runs-on: ${{ matrix.runner }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          submodules: recursive

      - name: Build ${{ matrix.target }} RPM packages
        env:
          RPM_DISTRO: ${{ matrix.distro }}
          BASE_IMAGE: ${{ matrix.base_image }}
        run: ./packaging/rpm/build-flagcx-rpm.sh ${{ matrix.backend }} ${{ matrix.base_image_version }}

      - name: Upload ${{ matrix.target }} RPM packages
        uses: actions/upload-artifact@v7
        with:
          name: flagcx-${{ matrix.target }}-rpm-packages
          path: rpm-packages/${{ matrix.target }}/**/*.rpm
          retention-days: 7

  # Publish on the internal h20 runner, which can reach the internal Nexus.
  # Tag-only and never triggered by pull_request, so fork PRs are never handed
  # the Nexus credentials (GitHub withholds secrets from fork PRs anyway; the
  # explicit tag guard is a second layer).
  #
  # TODO(unverified): this is the first design where h20 pulls a same-run
  # artifact via download-artifact (results.actions blob, a different endpoint
  # than codeload). It cannot be validated until the first v* tag is pushed; if
  # h20's egress to that endpoint is also unreliable, move this step to an
  # internal host with stable GitHub access, or fix h20's public egress.
  publish-rpm-packages:
    needs: build-rpm-packages
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: h20

    strategy:
      fail-fast: false
      matrix:
        target: [nvidia, nvidia-openeuler2403, metax, ascend]

    steps:
      - name: Download ${{ matrix.target }} RPM packages
        uses: actions/download-artifact@v8
        with:
          name: flagcx-${{ matrix.target }}-rpm-packages
          path: rpm-packages/${{ matrix.target }}

      - name: Publish ${{ matrix.target }} RPMs to Nexus YUM repository
        env:
          NEXUS_USERNAME: ${{ secrets.REGISTRY_USERNAME }}
          NEXUS_PASSWORD: ${{ secrets.CONTAINER_REGISTRY }}
          NEXUS_REPO_URL: https://resource.flagos.net/repository/flagos-yum-hosted
          TARGET: ${{ matrix.target }}
        run: |
          set -euo pipefail

          uploaded=0
          while IFS= read -r -d '' rpm; do
            # rel keeps the RPMS/<arch>/<file> or SRPMS/<file> layout under the backend
            rel="${rpm#rpm-packages/${TARGET}/}"
            echo "Uploading ${rpm} -> ${TARGET}/${rel}"
            curl -f -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}" \
              --upload-file "$rpm" \
              "${NEXUS_REPO_URL}/${TARGET}/${rel}"
            uploaded=$((uploaded + 1))
          done < <(find "rpm-packages/${TARGET}" -name '*.rpm' -print0)

          if [ "$uploaded" -eq 0 ]; then
            echo "No RPMs found for ${TARGET}"
            exit 1
          fi
          echo "Uploaded ${uploaded} ${TARGET} RPM(s) to Nexus YUM repository"
